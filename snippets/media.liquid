
{% case media.media_type %}
  
 

  {% when 'image' %}
   <div class="swiper-slide" data-media-alt="{{media.alt}}">
  <div
    class="product-single__media"
    data-media-id="{{ media.id }}"
    data-media-alt="{{media.alt}}">
    {{ media | image_url: width: 400, height: 400 | image_tag }}
  </div>
 
   </div>
  
{% endcase %}

